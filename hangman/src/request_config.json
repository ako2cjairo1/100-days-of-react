{
	"model": "text-davinci-003",
	"temperature": 0.7,
	"max_tokens": 3000,
	"top_p": 1,
	"frequency_penalty": 0,
	"presence_penalty": 0,
	"prompt": "[return only the main response. remove pre-text and post-text.] Generate exactly 50 random english words and their definitions, use JSON format with these properties: 'id' as unique number, 'word' and 'definition' are both string types."
}
